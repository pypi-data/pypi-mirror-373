[project]
name = "django-scrapyd-manager"
version = "0.1.0"
description = "manage scrapyd node by django"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.5",
    "requests>=2.32.4",
]

[tool.setuptools.packages.find]
where = ["django_scrapyd_manager"]