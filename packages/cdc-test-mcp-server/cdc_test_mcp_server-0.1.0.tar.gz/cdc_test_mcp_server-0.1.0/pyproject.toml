[project]
name = "cdc-test-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sqzhen", email = "sqzhen@iflytek.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cdc-test-mcp-server = "cdc_test_mcp_server:main"

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"
