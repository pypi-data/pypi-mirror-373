[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "yflow"
version = "0.1.0"
description = "Ví dụ Hydra CLI"
readme = "README.md"
requires-python = ">=3.12.9"
authors = [{ name="Ban", email="ban@example.com" }]

[tool.hatch.build.targets.wheel]
packages = ["src/yflow"]

[project.scripts]
yflow = "yflow.main:controller"
