[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "CustomLists"              # CustomLists
version = "0.1.0"           # starting version
description = "My first Python library just created for education purpose"
readme = "README.md"
requires-python = ">=3.7"
authors = [{name="Aman Kumar", email="mr.aman12nov@gmail.com"}]
license = {text = "MIT"}     # or whatever license you choose
dependencies = []           # other packages your lib depends on
