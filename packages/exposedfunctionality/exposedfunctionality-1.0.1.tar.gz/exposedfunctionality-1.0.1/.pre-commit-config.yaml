repos:
  - repo: https://github.com/python-poetry/poetry
    rev: "1.8.0" # add version here
    hooks:
      - id: poetry-check
        verbose: true
      - id: poetry-lock
        verbose: true
      - id: poetry-install
        verbose: true
