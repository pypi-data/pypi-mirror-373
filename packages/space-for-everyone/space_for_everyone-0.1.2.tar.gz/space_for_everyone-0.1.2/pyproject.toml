[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "space-for-everyone"
version = "0.1.2"
description = "Host anywhere amd anytime - Instant Preview Link"
authors = [{name="Sid & Kan", email="sofiyasenthilkumar@gmail.com"}]
readme = "README.md"
license = {text="MIT"}
requires-python = ">=3.8"
dependencies = ["requests>=2.28"]

[project.scripts]
host = "host:main"
