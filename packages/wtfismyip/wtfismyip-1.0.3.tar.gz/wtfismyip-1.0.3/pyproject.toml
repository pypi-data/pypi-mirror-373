[build-system]
requires = [
    "setuptools",
    "setuptools_scm",
]
build-backend = "setuptools.build_meta"

[project]
name = "wtfismyip"
dynamic = ["version"]
description = "A simple command line tool to get your public IP address."
dependencies = [
    "requests",
    "rich",
]

authors = [
    { name = "Jorge Alberto Díaz Orozco (Akiel)", email = "diazorozcoj@gmail.com" },
]

readme = "README.md"
license = "MIT"

[project.scripts]
wtfismyip = "wtfismyip:main"

[tool.setuptools_scm]