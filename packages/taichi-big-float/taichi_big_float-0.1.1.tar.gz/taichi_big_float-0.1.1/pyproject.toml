[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taichi-big-float"
version = "0.1.1"
description = "Multiprecision type extension for taichi"
readme = "README.md"
authors = [
  { name="Balázs Szalai", email="balazs.backup.2@gmail.com" }
]
requires-python = ">=3.8, <=3.12"
dependencies = [
    "taichi>=1.7",
    "mpmath"
]

[tool.setuptools.packages.find]
where = ["."]

[project.urls]
"Homepage" = "https://github.com/balazs-szalai/taichi-bigfloat/"
