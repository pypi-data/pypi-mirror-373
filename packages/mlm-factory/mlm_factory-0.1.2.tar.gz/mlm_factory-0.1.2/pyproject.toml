[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "mlm_factory"
version = "0.1.2"  # <- increment version
description = "A multimodal factory library"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="Wissem Karous", email="your-email@example.com" }
]
