[project]
name = "dian-utils"
version = "1.0.0"
description = "Validador de NIT colombiano con detección de patrones sospechosos"
authors = [{ name = "Juan Perea" }]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.7"

[project.urls]
Homepage = "https://github.com/jdperea/dian-utils"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
