[project]
name = "filespawn"
version = "0.1.2"
description = ""
authors = [
    {name = "Ang Aik Meng",email = "aikmeng@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "filespawn", from = "src"}]

[tool.poetry.scripts]
filespawn = "filespawn.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
