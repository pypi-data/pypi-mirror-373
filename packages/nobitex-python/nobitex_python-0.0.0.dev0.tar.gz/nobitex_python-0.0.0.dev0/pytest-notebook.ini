# pytest.ini configuration for notebook-only testing
[pytest]
pythonpath = .
testpaths = tests
addopts = -v --nbval-lax

# Configure notebook testing
nbval_ignore_cells = ["# IGNORE", "# SKIP"]
nbval_timeout = 600
