[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "intputlib"
version = "1.1.3"
description = "A simple helper function for reading integers and floats from input"
authors = [{ name = "Kayk Caputo", email = "caputo.kayk@gmail.com" }]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
