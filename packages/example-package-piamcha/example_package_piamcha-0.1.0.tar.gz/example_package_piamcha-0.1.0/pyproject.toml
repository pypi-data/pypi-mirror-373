[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "example-package-piamcha"
version = "0.1.0"
description = "A simple example Python package"
authors = [{name = "Tae", email = "pirakorn.iamcharernying@gmail.com"}]
readme = "README.md"
license = {text = "MIT"}
dependencies = []
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]