[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "evalcards"
version = "0.2.6"
description = "Reportes de evaluación (clasificación/regresión) a Markdown con gráficos"
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "Ricardo Urdaneta", email = "ricardourdanetacastro@gmail.com" }]
license = { text = "MIT" }
dependencies = ["numpy>=1.22","pandas>=1.5","scikit-learn>=1.3","matplotlib>=3.7","jinja2>=3.1"]

[tool.hatch.build.targets.wheel]
packages = ["src/evalcards"]

[project.scripts]
evalcards = "evalcards.report:main"

[project.urls]
Homepage = "https://github.com/Ricardouchub/evalcards"