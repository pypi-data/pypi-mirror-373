[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pykatex"
version = "0.0.1"
description = "Reserved package name - work in progress"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Ruben van Nieuwpoort", email = "ruben@vannieuwpoort.dev" }]

classifiers = [
    "Development Status :: 1 - Planning",
    "Programming Language :: Python :: 3",
]

[tool.setuptools]
packages = []
