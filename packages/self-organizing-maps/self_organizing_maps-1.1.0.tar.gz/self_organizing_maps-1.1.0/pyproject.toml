[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "self-organizing-maps"
version = "1.1.0"
description = "Unsupervised topological learning preserving neighborhood relationships, modeling biological cortical organization"
readme = "README.md"
license = {text = "Custom Non-Commercial License with Donation Requirements"}
authors = [
    {name = "Benedict Chen", email = "benedict@benedictchen.com"},
]
maintainers = [
    {name = "Benedict Chen", email = "benedict@benedictchen.com"},
]
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.21.0",
    "scipy>=1.7.0", 
    "scikit-learn>=1.0.0",
    "matplotlib>=3.5.0",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-cov>=4.0", 
]

[project.urls]
Funding = "https://www.paypal.com/cgi-bin/webscr?cmd=_s-xclick&hosted_button_id=WXQKYYKPHWXHS"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--cov=self_organizing_maps", "--cov-report=term-missing"]
[tool.hatch.build.targets.wheel]
py-modules = ["self_organizing_maps"]
only-include = ["*.py", "LICENSE", "README.md"]
