[project]
name = "beetle-logging"
version = "0.1.1"
description = "A python package with extra tools for the python logging package"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
beetle-logging = "beetle_logging:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
