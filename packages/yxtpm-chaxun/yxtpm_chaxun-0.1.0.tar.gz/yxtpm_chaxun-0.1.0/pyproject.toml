[project]
name = "yxtpm-chaxun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "zargabaath", email = "15122150377@163.com" }
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.13.1",
]

[project.scripts]
yxtpm-chaxun = "pubmed_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/pubmed_mcp"]
