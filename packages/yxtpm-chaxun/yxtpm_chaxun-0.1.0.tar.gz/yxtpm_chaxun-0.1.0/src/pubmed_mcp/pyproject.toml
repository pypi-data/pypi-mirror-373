[project]
name = "pubmed-mcp"
version = "0.1.0"
description = "PubMed search MCP server"
requires-python = ">=3.11"
dependencies = ["mcp[cli]>=1.0.0", "httpx>=0.27"]

[project.scripts]
pubmed-mcp = "pubmed_mcp.server:run"
