[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "anumoy_verma"   # Must be unique on PyPI
version = "0.1.0"
description = "My first Python package"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Anumoy Verma", email = "your-email@example.com"}]
requires-python = ">=3.8"
dependencies = []