from setuptools import setup
setup(
    name='greetings', # must match the package folder name
    version='1.0.0',
    description='A simple Hello World!',
    author = 'Fabio Nelli',
    install_requires=[] # dependencies
)