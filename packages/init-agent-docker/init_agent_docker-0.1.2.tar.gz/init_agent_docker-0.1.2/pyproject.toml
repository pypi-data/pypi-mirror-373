[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "init-agent-docker"
version = "0.1.2"
description = "init-agent-docker."
authors = [
    { name = "LauLe", email = "levanlau.net@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
