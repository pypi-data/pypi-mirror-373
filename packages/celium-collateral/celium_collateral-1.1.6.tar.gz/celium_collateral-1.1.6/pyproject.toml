[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "celium-collateral"
version = "1.1.6"
description = "Collateral Smart Contract for Lium"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Waris", email = "waris0609@outlook.com" }
]
requires-python = "<3.13,>=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent"
]
dependencies = [
    "web3>=6.0.0",  # compatible with eth-utils==2.2.2
    "requests>=2.32.3",
    "eth-utils==2.2.2",
    "scalecodec>=1.2.11"
]

[project.urls]
Homepage = "https://github.com/Datura-ai/celium-collateral-contracts"
Source = "https://github.com/Datura-ai/celium-collateral-contracts"

[tool.setuptools.packages.find]
where = ["."]
include = ["celium_collateral_contracts"]

[tool.setuptools.package-data]
celium_collateral_contracts = ["*.json"]

[tool.pdm]
