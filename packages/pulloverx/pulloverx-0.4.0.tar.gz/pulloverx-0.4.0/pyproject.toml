[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pulloverx"
version = "0.4.0"
description = "This is a lib for developing prank viruses"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"

dependencies = [
    "requests>=2.30.0",
    "pillow",
    "pyautogui",
    "keyboard"
]