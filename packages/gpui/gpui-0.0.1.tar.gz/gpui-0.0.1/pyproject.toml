[project]
name = "gpui"
version = "0.0.1"
description = "Zed's GPUI for Python"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [{ name = "Lex Berezhny", email = "lex@damoti.com" }]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

