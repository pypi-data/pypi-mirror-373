[project]
name = "big_xom_library"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["Project"]       # 从Project目录开始搜索
include = ["*"]           # 包含所有包（*匹配所有）
exclude = []     