[project]
name = "sushi-train"
version = "0.1.4"
description = "Like a sushi train, but for data engineering. Grab a utility function when you want it, and leave it if you don't."
license = {text = "MIT"}
authors = [
    {name = "MichaelGalo",email = "michael.galo.dev@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "azure-storage-blob>=12.26.0",
    "duckdb>=1.3.2",
    "minio>=7.2.16",
    "polars>=1.32.3",
    "requests>=2.32.5",
    "sqlalchemy>=2.0.43",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4.1",
    "ruff>=0.12.11",
]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sushi-train"]

[project.urls]
Homepage = "https://github.com/MichaelGalo/sushi-train"
