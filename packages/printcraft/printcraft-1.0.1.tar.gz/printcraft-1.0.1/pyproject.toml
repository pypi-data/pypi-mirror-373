[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "printcraft"
version = "1.0.1"
description = "A pretty-printing library for dicts, lists, tables, and JSON."
readme = "README.md"
license = "MIT"
authors = [
  {name = "Sayed Afaq Ahmed", email = "sayedafaq.contact@gmail.com"}
]
requires-python = ">=3.8"
