[build-system]
requires = ["setuptools"]

[project]
name = "splints"
version = "0.0.3"
dependencies = ["pyyaml", "pygls == 1.3.1"]

[project.scripts]
splints = "splints.server:run"

[tool.setuptools.package-dir]
splints = "splints"
