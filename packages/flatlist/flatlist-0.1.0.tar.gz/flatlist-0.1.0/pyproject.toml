[build-system]
requires = ["setuptools>=61.0", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "flatlist"            # change if name already taken
version = "0.1.0"
description = "A simple Python package to flat a list"
readme = "README.md"
requires-python = ">=3.8"
authors = [{name="SAJAL", email="sajalkumar801@gmail.com"}]
license = {text = "MIT"}
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
