[project]
name = "flashplot"
version = "0.0.5"
dependencies = [
  "numpy",
  "matplotlib",
  "pillow"
]
requires-python = ">= 3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
