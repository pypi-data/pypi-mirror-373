[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "viper-utils"
version = "0.1.0"
description = "My first Python utility package with basic math functions"
authors = [
  { name = "Viper", email = "kidanutessema@gmail.com" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"