[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tokyo_devpe"
version = "1.0.0"
description = "Dummy tokyo_devpe package for PyPI"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
