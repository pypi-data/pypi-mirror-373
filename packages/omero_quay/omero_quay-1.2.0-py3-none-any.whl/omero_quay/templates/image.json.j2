        {
            "@id": "{{ image["urls"][0] }}",
            "@type": "File",
            "name": "{{ image["name"] }}",
            "description": "{{ image["description"] }}",
            "dateCreated": "{{image["creation_date"]}}",
            "proprietaryName": "{{image["owner"]}}",
            "thumbnailUrl": "",
            "hasDefinedTerm": [{% for annotation_id in image["quay_annotations"] -%}{"@id": "{{ annotation_id }}"}{% if not loop.last %},{% endif %}{%- endfor %}]
        }
