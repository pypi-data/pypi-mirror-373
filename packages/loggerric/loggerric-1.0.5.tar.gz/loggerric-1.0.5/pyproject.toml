[project]
name = "loggerric"
version = "1.0.5"
description = "A fancy logging, progress bar, prompt, pretty printing, and timer utility for Python. Developed for personal use."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name="Luka Jacobsen" }]
license = { file="LICENSE" }

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"