"""End-to-end orchestration (fill in as needed)."""
from pathlib import Path
import pandas as pd

def run_pipeline(study_dir: str | Path, feature_type: str = "ecdf", model_path: str | Path | None = None):
    """Wire: read -> preprocess -> segment -> features -> (model) -> outputs.
    This is a placeholder; connect your functions once you've reviewed the split.
    """
    raise NotImplementedError("Connect your modules here after reviewing the scaffold.")
