[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "x_all_modules"
version = "0.1.0"  # 👈 this is the missing/invalid part
authors = [
    { name = "Ashutosh Dwivedi", email = "iasashutosh17@gmail.com" }
]
description = "It is a module which, if you import then you won't need to import any other module for any purpose..."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
