{
  "$id": "kafka/risk.limit.breached.v1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "risk.limit.breached@v1",
  "type": "object",
  "required": ["event_id","version","account_id","limit_id","metric","current_value","limit_value","ts"],
  "properties": {
    "event_id": {"type":"string"},
    "version": {"const":1},
    "account_id": {"type":"string"},
    "limit_id": {"type":"string"},
    "metric": {"type":"string"},
    "current_value": {"type":["string","number"]},
    "limit_value": {"type":["string","number"]},
    "reason": {"type":"string"},
    "ts": {"type":"string","format":"date-time"}
  },
  "additionalProperties": false
}
