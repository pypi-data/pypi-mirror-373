[project]
name = "weighted-buzzwords"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jonathan graf", email = "jonathangraf@outlook.de" }
]
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib",
    "squarify>=0.4.4",
]

[project.scripts]
weighted-buzzwords = "weighted_buzzwords:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
