from setuptools import setup, find_packages

setup(
    name="atikin_click",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # agar koi dependencies hai to
    ],
    python_requires='>=3.8',
)
