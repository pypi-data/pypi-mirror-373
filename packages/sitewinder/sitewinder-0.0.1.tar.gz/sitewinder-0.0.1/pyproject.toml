[project]
name = "sitewinder"
version = "0.0.1"
description = "Angular-like components in pure Python with PyScript + pyhtml5."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyhtml5"
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
