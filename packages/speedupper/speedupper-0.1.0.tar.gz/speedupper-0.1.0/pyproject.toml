[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "speedupper"
version = "0.1.0"
description = "Simple FPS booster for Python loops"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.7"