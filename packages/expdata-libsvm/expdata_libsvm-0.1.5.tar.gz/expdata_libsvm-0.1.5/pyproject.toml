[project]
name = "expdata_libsvm"
version = "0.1.5"
description = "datasets"
readme = "README.md"
authors = [
    { name="Junshan Yin", email="junshanyin@163.com" }
]
dependencies = [
    "numpy==2.2.6",
    "scikit-learn==1.7.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/expdata_libsvm"]
