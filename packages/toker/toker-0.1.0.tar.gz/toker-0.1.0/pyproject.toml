[project]
name = "toker"
version = "0.1.0"
description = "Extremely fast tokenizer for language modeling"
readme = "README.md"
authors = [
    { name = "Marcel Rød", email = "marcelroed@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
toker = "toker:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"
